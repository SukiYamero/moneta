# Wave 2 track reports

One file per track, owned by that track alone (`docs/wave-2-plan.md` §1.2).
The operator folds each into `specs.md` / `docs/waves.md` / the directory
`README.md`s after the track merges, then leaves the file here as the raw
record of what that agent actually reported.

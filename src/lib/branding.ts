// Display brand only — provisional and expected to change (specs.md §11).
// Storage identifiers must NOT track it: the Drive folder name, the dexie DB
// name and the lock HKDF info are frozen — renaming those orphans user data.
export const APP_NAME = 'KuroBello'

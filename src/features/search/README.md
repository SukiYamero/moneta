# src/features/search

- `SearchScreen.tsx` — route-level screen mounted at `/search`
  (`src/router.tsx`, inside `RequireAuth`). Currently a minimal, honest
  placeholder (title + back link to `/`) — Track E3 (Wave 2 stage 3)
  replaces the body. Stable contract for that handoff: named export
  `SearchScreen`, no props. Copy comes from the `search`/`common` i18n
  namespaces (`src/lib/i18n`).

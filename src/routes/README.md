# src/routes

Route-level page components, wired into `src/router.tsx`.

- `Home.tsx` — the only route right now; hosts `LockSettings` as a dev
  harness. Will host the dashboard/movimientos entry points once those land.
